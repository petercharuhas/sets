/* Question 1 */
// {1,2,3,4}

/* Question 2 */
// "ref"

/* Question 3 */
// 0: {[1, 2, 3] => true}
// 1: {[1, 2, 3] => false}

/* Question 3 */
// const hasDuplicate = (arr) => arr.some((value, index) => arr.indexOf(value) !== index);

/* Question 4 */
/* const vowelCount = (str) => {
  const vowels = "aeiou";
  const vowelMap = new Map();

  for (const char of str.toLowerCase()) {
    if (vowels.includes(char)) {
      vowelMap.set(char, (vowelMap.get(char) || 0) + 1);
    }
  }

  return vowelMap;
};
*/

